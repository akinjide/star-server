{
  "name": "star-server",
  "scripts": {
    "migrate:up": "db-migrate up --verbose",
    "migrate:down": "db-migrate down --verbose",
    "migrate:reset": "db-migrate reset --verbose",
    "migrate:print": "db-migrate up --dry-run --verbose",
    "start": "node src/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Akinjide Bankole <r@akinjide.me>",
  "devDependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "compression": "^1.7.4",
    "config": "^3.3.11",
    "date-fns": "^3.6.0",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "winston": "^3.13.0"
  }
}
